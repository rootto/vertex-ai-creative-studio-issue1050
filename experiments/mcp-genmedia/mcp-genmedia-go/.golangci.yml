linters-settings:
  govet:
    enable-all: true
    disable:
      - fieldalignment

linters:
  disable-all: true
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - typecheck
    - bodyclose
    - copyloopvar

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
