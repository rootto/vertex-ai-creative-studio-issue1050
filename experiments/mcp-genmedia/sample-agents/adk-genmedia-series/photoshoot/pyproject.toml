[project]
name = "photoshoot"
version = "0.1.0"
description = "ADK crawl-tier agent: turn a terse shot idea into an art-directed image via nanobanana"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.0",
    "google-adk[gcp,mcp]>=2.8.0",
]
